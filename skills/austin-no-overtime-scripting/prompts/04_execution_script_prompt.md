# 04 分段执行脚本Prompt

请把 Topic Card 转化为 3-5 分钟真人口播小窗 + AI实操录屏演示的分段执行脚本。

输出必须是表格，字段包括：

`#`、`时间段`、`段落目的`、`口播轨`、`画面/录屏轨`、`字幕重点`、`后期提示`、`人工QA点`

硬性要求：

1. 开头8秒必须给结果或冲突。
2. 中段实操最多3步。
3. 必须出现旧流程、新流程、AI介入点。
4. 必须出现人工修正点或AI边界。
5. 结尾必须回到真人判断；可提出下一条或可选沉淀方向，但不要强行指定资产。
6. 语气自然，不要像课程讲义。
