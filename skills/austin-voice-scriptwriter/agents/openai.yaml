interface:
  display_name: "Austin口播写作"
  short_description: "把选题写成 Austin 真人口播稿"
  default_prompt: "Use $austin-voice-scriptwriter to develop this confirmed topic from its available materials into a complete Austin article, using the optional third-party craft reference only where it helps the current material, then adapt the finished article into an editable spoken draft; add first-party public research when concrete support is missing."
policy:
  allow_implicit_invocation: true
