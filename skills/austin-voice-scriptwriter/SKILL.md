---
name: austin-voice-scriptwriter
description: 将已确认的 AI 账号选题、Topic Card、制作方向补充或 06 执行包输入，写成接近 Austin 真人口播习惯的全文。适用于生成短视频口播稿、提词器稿、真人开场、痛点展开、旧流程对比、三步动作、边界收尾。优先用于 06 完整脚本包中的“口播全文”，不负责选题筛选、事实核验、飞书回写或自动发布。
---

# Austin Voice Scriptwriter

这个 Skill 专门负责“口播全文怎么像 Austin 说出来”，不负责 `04 -> 06` 流程调度。

## 何时使用

- 已经有 `04 工作流实验命题卡`、Topic Card、执行包输入或人工制作补充。
- 用户要生成完整口播稿、提词器稿、开头钩子、痛点展开或真人讲述段。
- 原稿看起来像 PRD、课程讲义、AI 汇报或系统说明，需要改成真实口播。

不要用它做选题判断、事实核验、飞书字段整理、素材清单或剪辑交接。这些仍由 `austin-no-overtime-scripting` 处理。

## 写作顺序

1. 先读该题自己的 `selection_reason`、`persona_fit`、`source_facts`、理解包和事实边界，
   找出只有这条题才成立的判断或冲突。
2. 根据材料选择叙事骨架。可以从一次判断变化、责任链、失败假设、现场复盘、对照实验或
   一个具体动作进入；不要默认连续提问、旧流程、三步清单或统一边界结尾。
3. 只有输入提供 Austin 本人现场、人工补充或明确制作方向时，才能写成真实第一人称经历。
   没有亲历事实时，可以写“我看到这里会先问”“我建议这样验证”，不能虚构已经做过。
4. 来源事实、Austin 判断、建议测试、发布前核验分别写清，不把观点说成来源结论，也不把
   建议动作写成已完成结果。
5. 抽象判断后接当前输入支持的具体对象、动作或证据；没有具体现场时保持克制，不补通用
   “一早打开电脑/十几条待确认/客户现场”。
6. 每个实操点先用人话，再补工具、字段、Agent 或工作流；概念和工具不能抢走人的判断。
7. 整批完成后并排检查：两条不同选题不能共享实质相同的开场、推进顺序、章节标签、
   “第一/第二/第三”骨架、过渡句或通用“AI不是万能”收尾。

## 必须像口播

- 句子要短，适合直接念。
- 允许重复关键词，重复会让口播更自然。
- 抽象判断后必须接具体现场。
- 知识库、RAG、Agent、TTS、Voice Agent、工作流系统、AI 工具等概念不要一上来当主角；先让观众听懂旧方式为什么卡，再自然引出概念。
- 可以用“你可能也有这种感觉”“你看”“说白了”“这才是”做转场。
- 不要把自己写成老师，要像一线实战分享。

## 可借用语气

- 可以借 Austin 的短句、反问、停顿和真实判断感，但不能连续套用同一批句式。
- 可以写“最痛苦的不是 A，而是 C”，但 A/C 必须来自当前选题的真实旧流程和证据。
- 可以写“以前是 X；现在是 Y”，但 X/Y 必须来自输入材料，不能每条都走同一套三动作。
- 可以用“当然，它不是万能的”收边界，但边界要具体到当前工具、事实核验或拍摄素材。
- 这句话只是可选语气，不是必需结尾；更优先使用当前选题独有的责任或事实边界收束。
- 禁止为了像口播而复用固定金句；两个不同选题不应共享同一套章节名、推进骨架和过渡句。

## 禁止口吻

- 不要写“本条视频的核心观点是”“这一段要让观众相信”“本次验证路径为”。
- 不要把 `验证方式`、`沉淀资产`、`QA`、`执行包`、`进入06` 这些内部词放进口播。
- `沉淀资产` 也不能进入开头钩子、标题、封面大字、简介、置顶评论、素材清单或 QA 原因；统一改成人话：`有没有留下来`、`下次还能不能用`、`路径有没有串起来`、`后面能不能复用`、`资料有没有变成下次能用的东西`。
- `如果当天/今天没有生成 06`、`没有完整生成到最后一步`、`选题系统复盘` 这类内部状态边界不能写进口播、拍摄待办、素材清单、剪辑节奏或发布文案。
- 不要为了完整而解释所有字段。
- 不要照搬对标账号、热点标题或工具卖点；外部表达只能被拆解后转成自己的业务语言。
- 不要把工具能力说成已经验证过，除非输入里明确有证据。
- 不要把建议案例写成已经发生的真实案例。
- 不要把 approved private reference 或历史优质稿中的事实、经历、客户、工具测试和结果
  移植到当前稿；它们只能帮助判断节奏、结构和自然表达。
- 不要把用户临时举例写成固定规则；不同选题要根据输入里的真实旧流程、痛点和素材选择讲法。

## 资源

- 需要更完整的公开安全风格规则时，读 `references/public_voice_style.md`。
- 如果全局私有版存在 `references/private/three_round_learning.md`，需要模仿 Austin 最新口播风格时优先读它；仓库脱敏版不包含这个私有学习文档。
- `scripts/austin_voice.py` 只提供 `fallback_draft / not_style_qa` 的字段化兜底，用于格式、安全和边界检查；不得把它当成 Austin 风格质量样例。
- WEB-010 由当前 outer Codex 直接读取 approved Skill 与 references，并分别执行一次 batch；
  禁止在该自动化内再次启动 `codex exec` 或第二 Agent。
- 真实内容质量验收必须并排查看整批样例，由 PM、测试线程或用户人工判断；固定字数、
  关键词和模板字段不能替代风格验收。
